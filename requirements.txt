# AutoPlayOSU! 依賴套件
# 安裝方式: pip install -r requirements.txt
# 注意: torch 需要從 PyTorch 官網安裝對應 CUDA 版本
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

torch>=2.0
torchvision>=0.15
torchaudio>=2.0
numpy>=1.24
opencv-python>=4.7
mss>=9.0
keyboard>=0.13
mouse>=0.7
tqdm>=4.64
pywin32>=306
imbalanced-learn>=0.10
tensorboard>=2.0
scikit-learn>=1.0